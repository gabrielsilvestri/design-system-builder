Subtotal divider inside a card — separates groups of rows (Fixos / Variáveis).

```jsx
<Group label="Fixos" total="R$ 5.700" />
<Rows>{/* the fixed-cost rows */}</Rows>
```

Sits above the `Rows` block it heads. Accent uppercase label left, bold tabular total right.
