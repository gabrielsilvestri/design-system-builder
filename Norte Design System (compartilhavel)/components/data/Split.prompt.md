Two-proportion bar — side A blue (accent), side B violet (accent-2). Reads a 50/50 split of who contributed.

```jsx
<Split a={50} />
```

`a` is side-A percent; `b` defaults to the remainder. Use under a pair of Rows to visualize the proportion; pair with `Crown` to mark the bigger contributor.
