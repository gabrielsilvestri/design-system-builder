Single segmented gauge — "the tank". One accent fill read through a tick overlay.

```jsx
<Meter value={55} />
```

`value` is a 0–100 percentage. Pair it with a `Value variant="attention"` above and a `Sub` caption below ("já entraram R$ 10.000 · 55% · meta R$ 18.000").
