Violet glowing crown — the marker for who did more, set inline next to a value.

```jsx
<Row k="Pessoa B" v={<>R$ 12.000 <Crown /></>} variant="accent2" />
```

Purely a flag; no logic. Default 23px, override with `size`.
